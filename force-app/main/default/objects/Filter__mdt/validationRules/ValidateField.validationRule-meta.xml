<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ValidateField</fullName>
    <active>true</active>
    <errorConditionFormula>OR(
    AND(
        NOT(ISBLANK(SObjectField__c)),
        NOT(ISBLANK(Field_Name__c))
    ),
    AND(
        ISBLANK(SObjectField__c),
        ISBLANK(Field_Name__c)
    )
)</errorConditionFormula>
    <errorMessage>A Filter must have only one of the following:
- SObjectField
- Field Name</errorMessage>
</ValidationRule>
